// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 17fec9a34eb5

use crate::generated::types::*;
use crate::{Client, Result};
use reqwest::Method;
use serde::{Deserialize, Serialize};

/// Generated from the ArchAstro OpenAPI contract.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct GetApiV1AuthAllowedAuthMethodsResponseDataItem {
    /// One-sentence user-facing explanation of how this auth method works, suitable for display in an auth selection UI.
    pub description: String,
    /// Short user-facing label suitable for buttons or list items, e.g. `"Password"` or `"Magic Link"`.
    pub name: String,
    /// Stable machine-readable identifier for this auth method, e.g. `"password"` or `"magic_link"`. Use this value when enabling or referencing auth methods programmatically.
    pub slug: String,
}

/// Successful response
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct GetApiV1AuthAllowedAuthMethodsResponse {
    /// Ordered array of supported auth method objects, each with a stable slug, display name, and description.
    pub data: Vec<GetApiV1AuthAllowedAuthMethodsResponseDataItem>,
}

/// Authenticates a user with an email address and password and returns a short-lived
/// access token, a refresh token, and the authenticated user object. Use the refresh
/// token with the `/auth/refresh` endpoint to obtain new access tokens without
/// re-authenticating.
///
/// Password login must be enabled for the app; apps that have disabled password
/// authentication return HTTP 403. Requests are rate-limited per IP (10 per minute)
/// and per email-IP pair (5 per minute) — exceeding either limit returns HTTP 429.
///
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct PostApiV1AuthLoginInput {
    /// Email address of the user to authenticate.
    pub email: String,
    /// Password for the account associated with the given email.
    pub password: String,
}

/// Sends a magic link to the given email address so an existing user can sign in
/// without a password. The user clicks the link in their email and is redirected to
/// `redirect_uri` with a token; pass that token to `/auth/verify_link` to obtain
/// session tokens.
///
/// If no account exists for the email, the endpoint still returns success to prevent
/// email enumeration — no link is sent in that case. Both `email` and `redirect_uri`
/// are required. Requests are rate-limited per IP (10 per minute) and per email-IP pair
/// (3 per minute) — exceeding either limit returns HTTP 429. Returns HTTP 204 on success.
///
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct PostApiV1AuthLoginLinkInput {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// Email address of the account to send the magic link to.
    pub email: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// URL the user is redirected to after clicking the magic link. The token is appended as a query parameter.
    pub redirect_uri: Option<String>,
}

/// Exchanges a valid refresh token for a new access token and a new refresh token,
/// rotating the refresh token on every call. The response also includes the updated
/// user object. Store the new refresh token and discard the old one.
///
/// Refresh tokens are single-use — submitting an already-consumed token returns HTTP 401.
/// Rate limiting is applied per (user, IP) pair when the token can be verified, and
/// falls back to IP-only when it cannot. The limit is 30 exchanges per minute per
/// bucket; exceeding it returns HTTP 429.
///
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct PostApiV1AuthRefreshInput {
    /// Refresh token previously issued by a login, registration, or token-refresh response.
    pub refresh_token: String,
}

/// Creates a new user account and returns an access token, refresh token, and the new
/// user object. Two registration paths are supported:
///
/// - **Team registration**: supply `team_invite` with a valid team invite ID. The new
///   user is added to that team immediately upon registration. Returns HTTP 404 if the
///   invite is not found.
/// - **Standard registration**: supply `password`. An `invite_code` may optionally be
///   included for invite-gated apps; an invalid code returns HTTP 404.
///
/// Exactly one of `team_invite` or `password` must be provided; omitting both returns
/// HTTP 400. Password registration must be enabled for the app; disabled apps return
/// HTTP 403. The response status is HTTP 201 on success.
///
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct PostApiV1AuthRegisterInput {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// Display alias (handle) for the new account.
    pub alias: Option<String>,
    /// Email address for the new account.
    pub email: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// Full name for the new account.
    pub full_name: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// Invite code for invite-gated registration. Applied only in the standard registration path.
    pub invite_code: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// Password for the new account. Required for standard (non-team-invite) registration.
    pub password: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// Create or reuse an organization from the work-email domain and stamp the new user into it.
    pub set_org: Option<bool>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// Team invite ID. When provided, the user is added to the team on registration.
    pub team_invite: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// IANA timezone name for the new account, e.g. `"America/New_York"`.
    pub timezone: Option<String>,
}

/// Starts a passwordless registration flow by sending a verification link to the given
/// email address. The recipient clicks the link and is redirected to `redirect_uri` with
/// a token; pass that token to `/auth/verify_link` to complete registration and obtain
/// session tokens.
///
/// Profile fields (`full_name`, `alias`, `timezone`) are captured now and applied when
/// the link is verified. Requests are rate-limited per IP (10 per minute) and per
/// email-IP pair (3 per minute) — exceeding either limit returns HTTP 429. Returns
/// HTTP 204 on success.
///
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct PostApiV1AuthRegisterLinkInput {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// Display alias (handle) for the new account.
    pub alias: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// Email address to send the registration magic link to.
    pub email: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// Full name for the new account.
    pub full_name: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// URL the user is redirected to after clicking the registration link. The token is appended as a query parameter.
    pub redirect_uri: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// Create or reuse an organization from the work-email domain during confirmation.
    pub set_org: Option<bool>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// IANA timezone name for the new account, e.g. `"America/New_York"`.
    pub timezone: Option<String>,
}

/// Sends a passwordless magic link to the given email address. If an account with that
/// email already exists, a login link is sent. If no account exists, a registration link
/// is sent and the recipient completes sign-up by clicking through. This unified endpoint
/// lets you implement a single email-entry UI that handles both cases transparently.
///
/// The `redirect_uri` is validated against the app's registered hosts; an unregistered
/// URI returns HTTP 400. Both `email` and `redirect_uri` are required. Requests are
/// rate-limited per IP (10 per minute) and per email-IP pair (3 per minute). Returns
/// HTTP 204 on success — no body.
///
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct PostApiV1AuthRequestLinkInput {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// Email address to send the magic link to.
    pub email: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// URL the user is redirected to after clicking the magic link. Must be registered with the app.
    pub redirect_uri: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// For a new user, create or reuse an organization from the work-email domain during confirmation.
    pub set_org: Option<bool>,
}

/// Consumes a single-use login token delivered via email and returns an access token,
/// refresh token, and the authenticated user object. One-time tokens are issued by the
/// passwordless login flow and expire after a short window; submitting an expired or
/// already-used token returns HTTP 401.
///
/// If `timezone` is provided and the user's current timezone is still the default
/// (`"America/Los_Angeles"`), the account timezone is updated in the same request.
/// Requests are rate-limited to 10 per IP per minute; exceeding this returns HTTP 429.
///
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct PostApiV1AuthTokenInput {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// IANA timezone name to apply to the account if the account timezone is still the default, e.g. `"Europe/London"`. Omit to leave the timezone unchanged.
    pub timezone: Option<String>,
    /// Single-use login token extracted from the magic link or email code flow.
    pub token: String,
}

/// Consumes a single-use token from a magic link URL and returns an access token,
/// refresh token, and the authenticated user object. This endpoint completes both the
/// login flow (initiated by `/auth/request_login_link`) and the registration flow
/// (initiated by `/auth/request_register_link` or `/auth/request_link`).
///
/// Extract the token from the `token` query parameter of the magic link redirect URI
/// and POST it here. Expired or already-used tokens return HTTP 401 — expired links
/// carry the error code `expired_token`, unknown or already-used tokens carry
/// `invalid_or_expired_token`. If the app has disabled passwordless authentication
/// the request returns HTTP 403. Rate-limited to 10 requests per IP per minute —
/// exceeding this returns HTTP 429.
///
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct PostApiV1AuthVerifyLinkInput {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    /// Single-use magic link token extracted from the redirect URI query parameter.
    pub token: Option<String>,
}

/// Authentication API resource.
#[derive(Clone)]
pub struct Auth {
    pub(crate) client: Client,
}

impl Auth {
    /// List supported auth methods
    pub async fn get_api_v1_auth_allowed_auth_methods(
        &self,
    ) -> Result<GetApiV1AuthAllowedAuthMethodsResponse> {
        let path = "/api/v1/auth/allowed_auth_methods".to_owned();
        let request = self.client.request(Method::GET, &path);
        request.send().await
    }
    /// Blocking variant of [Self::get_api_v1_auth_allowed_auth_methods].
    #[cfg(feature = "blocking")]
    pub fn get_api_v1_auth_allowed_auth_methods_blocking(
        &self,
    ) -> Result<GetApiV1AuthAllowedAuthMethodsResponse> {
        crate::blocking::block_on(self.get_api_v1_auth_allowed_auth_methods())
    }
    /// Authenticate with email and password
    pub async fn post_api_v1_auth_login(
        &self,
        body: &PostApiV1AuthLoginInput,
    ) -> Result<AuthTokens> {
        let path = "/api/v1/auth/login".to_owned();
        let request = self.client.request(Method::POST, &path);
        let request = request.json(body)?;
        request.send().await
    }
    /// Blocking variant of [Self::post_api_v1_auth_login].
    #[cfg(feature = "blocking")]
    pub fn post_api_v1_auth_login_blocking(
        &self,
        body: &PostApiV1AuthLoginInput,
    ) -> Result<AuthTokens> {
        crate::blocking::block_on(self.post_api_v1_auth_login(body))
    }
    /// Request a magic link for login
    pub async fn request_login_magic_link(&self, body: &PostApiV1AuthLoginLinkInput) -> Result<()> {
        let path = "/api/v1/auth/login/link".to_owned();
        let request = self.client.request(Method::POST, &path);
        let request = request.json(body)?;
        request.send_empty().await
    }
    /// Blocking variant of [Self::request_login_magic_link].
    #[cfg(feature = "blocking")]
    pub fn request_login_magic_link_blocking(
        &self,
        body: &PostApiV1AuthLoginLinkInput,
    ) -> Result<()> {
        crate::blocking::block_on(self.request_login_magic_link(body))
    }
    /// Refresh an access token
    pub async fn post_api_v1_auth_refresh(
        &self,
        body: &PostApiV1AuthRefreshInput,
    ) -> Result<AuthTokens> {
        let path = "/api/v1/auth/refresh".to_owned();
        let request = self.client.request(Method::POST, &path);
        let request = request.json(body)?;
        request.send().await
    }
    /// Blocking variant of [Self::post_api_v1_auth_refresh].
    #[cfg(feature = "blocking")]
    pub fn post_api_v1_auth_refresh_blocking(
        &self,
        body: &PostApiV1AuthRefreshInput,
    ) -> Result<AuthTokens> {
        crate::blocking::block_on(self.post_api_v1_auth_refresh(body))
    }
    /// Register a new user with email and password
    pub async fn post_api_v1_auth_register(
        &self,
        body: &PostApiV1AuthRegisterInput,
    ) -> Result<AuthTokens> {
        let path = "/api/v1/auth/register".to_owned();
        let request = self.client.request(Method::POST, &path);
        let request = request.json(body)?;
        request.send().await
    }
    /// Blocking variant of [Self::post_api_v1_auth_register].
    #[cfg(feature = "blocking")]
    pub fn post_api_v1_auth_register_blocking(
        &self,
        body: &PostApiV1AuthRegisterInput,
    ) -> Result<AuthTokens> {
        crate::blocking::block_on(self.post_api_v1_auth_register(body))
    }
    /// Request a magic link for registration
    pub async fn request_register_magic_link(
        &self,
        body: &PostApiV1AuthRegisterLinkInput,
    ) -> Result<()> {
        let path = "/api/v1/auth/register/link".to_owned();
        let request = self.client.request(Method::POST, &path);
        let request = request.json(body)?;
        request.send_empty().await
    }
    /// Blocking variant of [Self::request_register_magic_link].
    #[cfg(feature = "blocking")]
    pub fn request_register_magic_link_blocking(
        &self,
        body: &PostApiV1AuthRegisterLinkInput,
    ) -> Result<()> {
        crate::blocking::block_on(self.request_register_magic_link(body))
    }
    /// Request a magic link for login or registration
    pub async fn request_magic_link(&self, body: &PostApiV1AuthRequestLinkInput) -> Result<()> {
        let path = "/api/v1/auth/request/link".to_owned();
        let request = self.client.request(Method::POST, &path);
        let request = request.json(body)?;
        request.send_empty().await
    }
    /// Blocking variant of [Self::request_magic_link].
    #[cfg(feature = "blocking")]
    pub fn request_magic_link_blocking(&self, body: &PostApiV1AuthRequestLinkInput) -> Result<()> {
        crate::blocking::block_on(self.request_magic_link(body))
    }
    /// Exchange a one-time login token for session tokens
    pub async fn exchange_login_token(&self, body: &PostApiV1AuthTokenInput) -> Result<AuthTokens> {
        let path = "/api/v1/auth/token".to_owned();
        let request = self.client.request(Method::POST, &path);
        let request = request.json(body)?;
        request.send().await
    }
    /// Blocking variant of [Self::exchange_login_token].
    #[cfg(feature = "blocking")]
    pub fn exchange_login_token_blocking(
        &self,
        body: &PostApiV1AuthTokenInput,
    ) -> Result<AuthTokens> {
        crate::blocking::block_on(self.exchange_login_token(body))
    }
    /// Verify a magic link token
    pub async fn verify_magic_link(
        &self,
        body: &PostApiV1AuthVerifyLinkInput,
    ) -> Result<AuthTokens> {
        let path = "/api/v1/auth/verify/link".to_owned();
        let request = self.client.request(Method::POST, &path);
        let request = request.json(body)?;
        request.send().await
    }
    /// Blocking variant of [Self::verify_magic_link].
    #[cfg(feature = "blocking")]
    pub fn verify_magic_link_blocking(
        &self,
        body: &PostApiV1AuthVerifyLinkInput,
    ) -> Result<AuthTokens> {
        crate::blocking::block_on(self.verify_magic_link(body))
    }
}

impl Client {
    /// Authenticate with email/password and enable generation-fenced automatic refresh.
    pub async fn with_credentials(
        api_key: impl Into<String>,
        email: impl Into<String>,
        password: impl Into<String>,
    ) -> Result<Self> {
        let client = Self::builder().publishable_key(api_key).build()?;
        let tokens = client
            .auth()
            .post_api_v1_auth_login(&PostApiV1AuthLoginInput {
                email: email.into(),
                password: password.into(),
            })
            .await?;
        client
            .install_session(
                tokens.token.clone(),
                Some(tokens.refresh_token.clone()),
                "/api/v1/auth/refresh",
            )
            .await;
        Ok(client)
    }
}
